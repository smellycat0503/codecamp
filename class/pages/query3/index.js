//0. 인덱스에서 컨테이너로 보내자. 리턴 뒤에 <Query /> 입력하기.
// 입력하면 자동으로 맨 위애 import 자동 완성됨.

import Query from '../../src2/components/query/write/QueryWrite.container';

export default function ProfilePage3() {
  return <Query />;
}
